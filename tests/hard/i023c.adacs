-- $I023C -- adapted from Babbage test #42
 
-- THIS IS FOR ALL OF YOU FOLDERS:
--    LET'S SEE YOUR GENERATED CODE !!
 
PACKAGE Main IS
    pragma TraceCodeGen;
BODY
    MANILA : CONSTANT INTEGER := 10;
    E      : CONSTANT REAL := 2.71828;
    N      : CONSTANT INTEGER := 10*MANILA;
    X      : ARRAY (5..2*5*2) OF INTEGER;
    I      : INTEGER;
Begin
    WRITE("Test $I023C (w/ output):"; NEXT);
    WRITE(MANILA*(MANILA-2*2*2);NEXT);
    I := (N+MANILA)*(N+MANILA);
    WRITE(I;NEXT);
    X(MANILA) := I+1*1*1*1*1*1*2*5;
    WRITE(X(5*4*3*2-N-MANILA));
 
END Main;
